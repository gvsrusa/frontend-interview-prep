{
  "type": "dsa",
  "title": "Quick Sort",
  "difficulty": "medium",
  "category": "sorting-searching",
  "language": "ts",
  "hints": [
    "Choose a pivot element (last element is simplest) and partition the array around it",
    "After partitioning, elements smaller than pivot are on the left, larger on the right",
    "Recursively sort the two partitions",
    "Lomuto partition uses a single scan; Hoare partition uses two converging pointers and is typically faster in practice"
  ],
  "timeLimit": 30,
  "languages": [
    "ts",
    "js"
  ]
}
